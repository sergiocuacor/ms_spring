package com.springbootapp.item.clientes;

import org.springframework.cloud.openfeign.FeignClient;

@FeignClient(name = "Productos") // Ponemos el nombre del microservicio con el que nos queremos comunicar(fijarnos en su application.properties)
public interface ProductoClienteRest {

}
